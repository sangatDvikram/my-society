/**
 * @society/shared-ui-components
 *
 * Root barrel export. Re-exports all web components so consumers can import
 * from the package root:
 *
 *   import { ClickCounter } from '@society/shared-ui-components'
 *
 * Platform-specific sub-path imports are also available:
 *   import { ClickCounter } from '@society/shared-ui-components/web'
 */

export * from './web'
